# Сценарио шалгалт

## B232270029, З.Анхбаяр

### Туршилтын тохиргоо

- **K6 version:** k6 v2.2.0 (commit/00a9a1b7f5, go1.26.5, linux/amd64)
- **Тест сервер:** [Github repository](https://github.com/Anhkbayar/test-server)

## Гурван сценарио

### 1. Performance сценарио

| Хэсэг                  | Тодорхойлолт                                                                                                                            |
| ---------------------- | --------------------------------------------------------------------------------------------------------------------------------------- |
| **Тойм**               | Хэвийн хэрэглэгчийн ачааллын үед `/cart/add` endpoint нь хурдан хариу өгч, хэрэглэгчийн сагсанд бараа нэмэх үйлдлийг саатуулахгүй байх. |
| **Системийн төлөв**    | Сервер хэвийн ажиллаж, `/cart/add` endpoint хүсэлт хүлээн авах боломжтой байна.                                                         |
| **Орчны төлөв**        | Орчн хэвийн, стандарт сүлжээний холболт                                                                                                 |
| **Гадаад өдөөлт**      | Хэрэглэгч сагсанд бараа нэмэх үйлдэл хийж `/cart/add` endpoint рүү хүсэлт илгээнэ.                                                      |
| **Шаардлагатай хариу** | Систем хүсэлтийг амжилттай боловсруулж, сагсанд барааг нэмэн HTTP амжилттай хариу буцаана.                                              |
| **Хэмжүүр**            | `/cart/add` endpoint-ийн response latency **p95 < 4 ms**, **p99 < 40 ms** байна.                                                        |

### 2. Reliability сценарио

| Хэсэг                  | Тодорхойлолт                                                                                                   |
| ---------------------- | -------------------------------------------------------------------------------------------------------------- |
| **Тойм**               | Хэвийн хэрэглээний үед хэрэглэгчийн төлбөрийн үйлдэл алдаатай дуусах давтамжийг бага түвшинд хадгалах.         |
| **Системийн төлөв**    | Сервер болон `/pay` endpoint хэвийн ажиллаж, хэрэглэгч төлбөр хийх боломжтой байна.                            |
| **Орчны төлөв**        | Орчн хэвийн, стандарт сүлжээний холболт                                                                        |
| **Гадаад өдөөлт**      | Хэвийн хэрэглэгч төлбөр хийх үйлдэл гүйцэтгэж `/pay` endpoint рүү хүсэлт илгээнэ.                              |
| **Шаардлагатай хариу** | Систем төлбөрийн хүсэлтийг боловсруулж, амжилттай төлбөрийн хариу эсвэл тодорхойлогдсон алдааны хариу буцаана. |
| **Хэмжүүр**            | `/pay` endpoint-ийн **error rate < 8%** байна.                                                                 |

### 3. Availability сценарио

| Хэсэг                  | Тодорхойлолт                                                                                                                              |
| ---------------------- | ----------------------------------------------------------------------------------------------------------------------------------------- |
| **Тойм**               | Сервер гэнэт crash болсон үед систем автоматаар дахин асаж, үйлчилгээг богино хугацаанд сэргээх.                                          |
| **Системийн төлөв**    | Сервер хэвийн ажиллаж, хэрэглэгчийн хүсэлтүүдийг боловсруулж байна.                                                                       |
| **Орчны төлөв**        | Орчн хэвийн, стандарт сүлжээний холболт                                                                                                   |
| **Гадаад өдөөлт**      | **ЭВДРЭЛ (crash):** сервер гэнэт зогсоно.                                                                                                 |
| **Шаардлагатай хариу** | Сервер автоматаар дахин асаж, үйлчилгээг сэргээн хэрэглэгчийн хүсэлтийг дахин боловсруулах боломжтой болно.                               |
| **Хэмжүүр**            | Бүх хүсэлтийн амжилтын хувь **p95 ≥ 90%**, **p99 ≥ 90%** байх ба серверийн сэргэх хугацаа **p95 ≤ 10 секунд**, **p99 ≤ 15 секунд** байна. |

### Алхам 2-ын өөрийн шалгалт

- **Итгэмжтэй:** Бодит хэрэглэгчийн үйлдэл болон бодит системийн эвдрэлийг өдөөлт болгон тодорхойлсон.
- **Үнэ цэнэтэй:** Хариу өгөх хугацаа, төлбөрийн алдаа, системийн зогсолт зэрэг хэрэглэгчид шууд нөлөөлөх үзүүлэлтүүдийг хэмжинэ.
- **Тодорхой:** VU, хүсэлт/секунд, туршилтын хугацаа болон эвдрэлийн хугацааг тодорхой заасан.
- **Нарийн:** Endpoint, өдөөлт, хүлээгдэж буй хариу болон хэмжүүр бүрийг тусад нь тодорхойлсон.
- **Ойлгомжтой:** Бүх сценарио ижил 6 хэсэгтэй бүтэцтэй бөгөөд хэмжүүрүүд нь тоон босготой.

---

## Сценарио бүрээс SLO гаргах

| Сценарио         | SLI (юуг хэмжих)            | Босго               | Цонх / нөхцөл                                        |
| ---------------- | --------------------------- | ------------------- | ---------------------------------------------------- |
| **Performance**  | `/cart/add` latency         | **p95 < 4 ms**      | **20 VU** тогтмол ачаалалд, **1 минут**              |
| **Reliability**  | `/pay` error rate           | **< 8%**            | **20 VU**, **1 минут**                               |
| **Availability** | Бүх хүсэлтийн амжилтын хувь | **p95 ≥ 90%**       | **20 VU**, **2 минут**, **10 секундийн crash** орсон |
| **Availability** | Сэргэх хугацаа              | **p95 ≤ 10 секунд** | **10 секундийн crash**-ийн дараах recovery           |

### SLO босго сонгосон шалтгаан

`/cart/add` endpoint-ийн **p95 < 4 ms** босгыг хэрэглэгчийн сагсанд бараа нэмэх үйлдэлд мэдэгдэхүйц саатал үүсгэхгүй байх зорилгоор сонгосон. `/pay` endpoint-ийн **8%-ийн error rate** босгыг туршилтын үед бага хэмжээний төлбөрийн алдаа гарах боломжийг тооцож, хэт хатуу бус боловч найдвартай ажиллагааг шалгах зорилгоор сонгосон. Availability-ийн **90%** босго нь 10 секундийн crash-ийг 2 минутын туршилтын цонхонд багтаан системийн recovery-г шалгах боломж олгоно.

### Availability Error Budget

Availability SLO:

**90%**

Туршилтын цонх:

**2 минут = 120 секунд**

Тиймээс зөвшөөрөгдөх downtime:

**120 × (1 − 0.90) = 12 секунд**

Иймээс availability-ийн **time-based error budget = 12 секунд** байна.

Энэ нь туршилтын 2 минутын цонхонд систем нийтдээ **12 секунд хүртэл** unavailable байхыг SLO зөвшөөрч байна гэсэн үг.

## Мини chaos

Серверийг туршилтын явцад **10 секундээр унтрааж**, availability-д үзүүлэх нөлөөг хэмжсэн.
Туршилтын нийт хугацаанд **5703 HTTP хүсэлт** илгээгдсэн. Сервер унтарсан үед хүсэлтүүд амжилтгүй болж, нийт `http_req_failed` **13.46%** болсон.

| Үзүүлэлт               |   Үр дүн |
| ---------------------- | -------: |
| Нийт HTTP хүсэлт       |     5703 |
| HTTP failure rate      |   13.46% |
| Checks                 |   86.53% |
| Availability SLO       |    > 90% |
| Availability threshold | **FAIL** |

Endpoint тус бүрийн үр дүн:

| Endpoint  | PASS | FAIL | Failure rate |
| --------- | ---: | ---: | -----------: |
| `/cart`   | 1677 |  224 |       11.78% |
| `/report` | 1670 |  231 |       12.15% |
| `/pay`    | 1588 |  313 |       16.47% |

Серверийг 10 секундээр зогсоосны улмаас хүсэлтүүдийн тодорхой хэсэг амжилтгүй болсон. Үүний үр дүнд `http_req_failed` өсөж, `checks` **86.53%** хүртэл буурсан.

Availability-ийн шаардлага нь:

`checks > 90%`

байхад хэмжилтийн үр дүн:

`checks = 86.53%`

болсон тул **Availability threshold FAIL** болсон.

> JSON output дээр `"rate>0.90": true` гэж гарсан боловч `checks.value = 0.8653` буюу 86.53% байна. 86.53% нь 90%-аас бага тул математик утгаараа threshold нь FAIL байх ёстой. Иймээс availability-ийн бодит үр **FAIL** болов.

### Error budget хэтэрсэн эсэх

Энэ үр дүнгээс харахад SLO-ийн 90%-ийн босго нь заавал хэт өөдрөг байсан гэж дүгнэж болохгүй. Харин 10 секундийн crash нь тухайн ачааллын үед олон хүсэлтэд зэрэг нөлөөлж, request-based availability-г 90%-аас доош оруулсан байна.

### Error budget

Availability SLO-г 90% гэж үзвэл 2 минутын туршилтын цонхонд зөвшөөрөгдөх downtime:
**120 × (1 − 0.90) = 12 секунд**

Иймээс time based error budget нь 12 секунд байна. Гэтэл

```
Actual downtime = 10 секунд
Error budget = 12 секунд
```

өөрөөр хэлбэл 10 секундийн зогсолт нь дангаараа 12 секундын time-based error budget-ийг хэтрээгүй. Гэхдээ хүсэлтээр хэмжсэн availability нь `86.53%` болсон тул SLO зөрчигдсөн.

Энэ ялгаа нь k6-ийн `checks` болон `http_req_failed` нь хугацаагаар бус хүсэлтийн тоогоор хэмжигддэгтэй холбоотой. Сервер хэвийн үед `/report` зэрэг endpoint-үүд 200–400 ms орчим хугацаа зарцуулдаг бол сервер crash болсон үед хүсэлтүүд `connection refused` зэрэг алдаагаар бараг шууд буцна. Тиймээс сервер унтарсан 10 секундийн хугацаанд нэг секундэд илүү олон хүсэлт илгээгдэж, тухайн 10 секунд нь нийт хүсэлтийн failure rate-д хугацааны харьцаанаас илүү их нөлөө үзүүлсэн.

Иймээс 12 секундийн time-based error budget болон `13.47`%-ийн request-based failure rate нь хоорондоо шууд адил утгатай биш юм.

### Availability ба reliability тусгаарлах

Availability SLI-г "хүсэлт серверт хүрч хариу авсан эсэх" (connection error, timeout, status 0-ээр) гэж, reliability SLI-г "хариу авсан хүсэлтүүдийн дотор зөв хариу (2xx, зөв body) буцсан эсэх" гэж тодорхойлбол crash зөвхөн availability-д, харин 5xx буцаадаг bug зөвхөн reliability-д нөлөөлж, хоёр SLI өөр өөр эвдрэлийг хэмжинэ.

## Threshold зориуд эвдэх

`/report` endpoint-ийн `p(95)<100` threshold-ийг шалгахын тулд `slo-test-fail.ts` скриптийг тусад нь үүсгэж ажиллуулсан.

### Threshold

```js
thresholds: {
  'http_req_duration{name: cart}': ['p(95)<50'],
  'http_req_duration{name: report}': ['p(95)<100'],
  'http_req_failed{name:pay}': ['rate<0.08'],
  checks: ['rate>0.90'],
}
```

### Үр дүн

| Endpoint    |  Threshold | Бодит p(95) | Үр дүн |
| ----------- | ---------: | ----------: | ------ |
| `/cart/add` |  `< 50 ms` |     2.19 ms | PASS   |
| `/report`   | `< 100 ms` |   390.48 ms | FAIL   |

`/report` endpoint-ийн бодит `p(95)` нь `390.48 ms` байсан тул `p(95)<100` threshold-ийг хангаагүй.

FAIL болсон үед k6-ийн гаралтад threshold зөрчигдсөн тухай мөр гарна:

```text
✗ 'p(95)<100' threshold on http_req_duration{name: report} has been crossed
```

Linux/macOS дээр exit code-ийг дараах командаар шалгасан:

```bash
echo $?
```

Threshold FAIL болсон үед k6:

```text
99
```

гэсэн exit code буцаана.

`99` нь k6 тест ажилласан боловч нэг буюу түүнээс олон threshold зөрчигдсөнийг илэрхийлнэ. CI pipeline энэ non-zero exit code-ийг ашиглан build-ийг амжилтгүй болгох боломжтой.

### Яагаад `/cart/add` PASS хэвээр байгаа вэ?

`/cart/add` endpoint нь localhost дээр маш хурдан хариулдаг. Хэмжилтээр `p(95)=2.19 ms` байсан бөгөөд энэ нь `50 ms`-ээс бага байна.

Харин `/report` endpoint нь сервер талдаа ойролцоогоор 200–400 ms зарцуулдаг тул `p(95)<100 ms` шаардлагыг хангаж чадахгүй.

Иймээс ижил `p(95)` хэлбэрийн threshold ашигласан ч endpoint бүрийн бодит response time өөр учраас `/cart/add` PASS, `/report` FAIL болж байна.

## Дүгнэлт

Энэ ажлаар performance, reliability, availability гэсэн гурван чанарын шинж чанарыг сценарио болгон тодорхойлж, тэдгээрээс тоон SLO гаргаад k6-ээр хэмжсэн. Мини chaos туршилтад серверийг 10 секунд унтраахад 5703 хүсэлтийн 13.47% амжилтгүй болж, checks 86.53% хүртэл буурсан тул 90%-ийн availability threshold FAIL болсон. Endpoint тус бүрээс /pay хамгийн өндөр буюу 16.47%-ийн алдаатай байсан. 10 секундийн зогсолт нь 12 секундийн time-based error budget-д багтсан ч request-based хэмжилтээр SLO зөрчигдсөн. Учир нь сервер унасан үед хүсэлтүүд connection refused-ээр бараг шууд буцаж, нэг секундэд илүү олон алдаатай хүсэлт бүртгэгддэг. Иймээс time-based болон request-based error budget нь шууд адил утгатай биш бөгөөд SLI-г ямар аргаар хэмжихээ эхнээс нь сонгох хэрэгтэйг харуулсан. Нэг эвдрэл availability болон reliability хоёр SLO-г зэрэг зөрчиж болох тул хоёр SLI-г тусгаарлах нь чухал юм. Availability-г "хариу ирсэн эсэх", reliability-г "ирсэн хариу зөв эсэх" гэж хуваавал crash болон 5xx bug өөр өөр хэмжигдэнэ. Threshold зориуд эвдэх туршилтаар /report-ийн бодит p(95) нь 390.48 ms байж 100 ms-ийн босгыг хангаагүй (FAIL), харин /cart/add нь 2.19 ms-ээр PASS болсон. Endpoint бүрийн бодит latency өөр тул threshold-ийг endpoint тус бүрт тусад нь тогтоох ёстой. Threshold зөрчигдөхөд k6 нь exit code 99 буцаадаг тул CI pipeline-д ашиглаж, SLO зөрчсөн build-ийг автоматаар зогсоох боломжтой.
